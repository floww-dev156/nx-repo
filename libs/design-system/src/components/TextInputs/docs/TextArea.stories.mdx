import { Meta } from '@storybook/addon-docs'

import { TextArea } from '../index.tsx'

<Meta title='MDX/TextArea' component={TextArea} />

export const Template = args => <TextArea {...args} />

**Version v1.0.0**

-  All sizes like xs, sm, md

## Backlogs

-  Handle different Focus ring colors from config
-  Need to resolve forwardedRef.current error
-  Auto new line on end of input line width
-  Need to add testId

## Future Versions

-  Need to handle drag both vertical and horizontal
