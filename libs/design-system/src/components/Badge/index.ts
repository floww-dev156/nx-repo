import { Badge } from './Badge';
import { badgeSizes } from './sizes';
import { Size, BadgeStyles, ColorVariantEnum } from './types';
import { BadgeSizesEnum, BadgeVariants } from './constants';
import {
  getBadgeStyles,
  getBadgeBorderStyles,
  getLeftAndRightPaddings,
} from './styles';

export {
  Badge,
  BadgeVariants,
  BadgeSizesEnum,
  getBadgeStyles,
  badgeSizes,
  getBadgeBorderStyles,
  getLeftAndRightPaddings,
};

export type { Size, BadgeStyles, ColorVariantEnum };
